<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/head') %>
</head>
<body data-page="404">

    <%- include('../partials/header') %>

    <section class="section-padding-lg" style="min-height: 80vh; display: flex; align-items: center; justify-content: center; text-align: center; padding-top: 150px;">
        <div class="container animate-on-scroll">
            <h1 style="font-size: clamp(6rem, 15vw, 10rem); font-weight: 800; background: var(--gradient-cta); -webkit-background-clip: text; -webkit-text-fill-color: transparent; margin-bottom: 0;">404</h1>
            <h2 style="font-size: 2rem; margin-bottom: 24px;">Oops! Page Not Found</h2>
            <p style="font-size: 1.2rem; color: var(--text-secondary); max-width: 600px; margin: 0 auto 40px;">The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.</p>
            <a href="/" class="btn btn-primary hover-lift"><i class="fas fa-home"></i> Back to Homepage</a>
        </div>
    </section>

    <%- include('../partials/footer') %>
    <%- include('../partials/whatsapp') %>

    <script src="/js/main.js"></script>
</body>
</html>
